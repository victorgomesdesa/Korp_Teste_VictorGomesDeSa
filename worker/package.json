{
  "name": "korp-voice-assistant",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm test && npm run typecheck && wrangler types --check && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8",
    "wrangler": "^4.124.0"
  }
}
